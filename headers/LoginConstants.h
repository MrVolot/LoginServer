#pragma once

enum credentialsStatus {
	WRONG_PASSWORD,
	RIGHT_PASSWORD,
	USER_ALREADY_EXISTS,
	SUCCESSFUL_REGISTRATION,
	AUTHORIZATION_SUCCEEDED,
	AUTHORIZATION_FAILED,
	GUEST_USER_CREATED_SUCCESSFULLY,
	AUTHENTICATION_IS_NEEDED,
	CORRECT_EMAIL_CODE,
	WRONG_EMAIL_CODE,
	ERROR_
};

enum requestType {
	REGISTRATION,
	LOGIN
};